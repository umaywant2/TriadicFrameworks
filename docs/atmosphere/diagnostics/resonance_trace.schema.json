{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Resonance Trace Schema (Atmosphere Module)",
  "description": "Schema for validating resonance_trace.json — oscillation memory, harmonic alignment, wave persistence, coherence envelopes.",
  "type": "object",

  "properties": {
    "trace": {
      "type": "object",
      "properties": {
        "name": { "type": "string" },
        "category": { "type": "string" },
        "version": { "type": "string" },
        "summary": { "type": "string" },
        "purpose": { "type": "string" }
      },
      "required": ["name", "category", "version", "summary", "purpose"]
    },

    "events": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "id": { "type": "string" },
          "description": { "type": "string" },
          "sequence": {
            "type": "array",
            "items": { "type": "string" }
          }
        },
        "required": ["id", "description", "sequence"]
      }
    },

    "signatures": {
      "type": "array",
      "items": { "type": "string" }
    },

    "agents": {
      "type": "object",
      "properties": {
        "primary": {
          "type": "array",
          "items": { "type": "string" }
        },
        "secondary": {
          "type": "array",
          "items": { "type": "string" }
        }
      },
      "required": ["primary", "secondary"]
    },

    "scales": {
      "type": "object",
      "properties": {
        "micro": { "type": "string" },
        "meso": { "type": "string" },
        "macro": { "type": "string" },
        "mega": { "type": "string" }
      },
      "required": ["micro", "meso", "macro", "mega"]
    },

    "fields": {
      "type": "object",
      "properties": {
        "primary_resonance_field": {
          "type": "array",
          "items": { "type": "string" }
        },
        "wave_memory_field": {
          "type": "array",
          "items": { "type": "string" }
        },
        "teleconnection_resonance_field": {
          "type": "array",
          "items": { "type": "string" }
        },
        "coherence_envelope_field": {
          "type": "array",
          "items": { "type": "string" }
        }
      },
      "required": [
        "primary_resonance_field",
        "wave_memory_field",
        "teleconnection_resonance_field",
        "coherence_envelope_field"
      ]
    },

    "cascades": {
      "type": "object",
      "additionalProperties": {
        "type": "array",
        "items": { "type": "string" }
      }
    },

    "summary": {
      "type": "object",
      "properties": {
        "core_capabilities": {
          "type": "array",
          "items": { "type": "string" }
        },
        "role": { "type": "string" }
      },
      "required": ["core_capabilities", "role"]
    }
  },

  "required": [
    "trace",
    "events",
    "signatures",
    "agents",
    "scales",
    "fields",
    "cascades",
    "summary"
  ]
}
