{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Dimensional Diagnostic Schema (Atmosphere Module)",
  "description": "Schema for validating dimensional_diagnostic.json — dimensional shifts, regime transitions, coherence breaks, structural transitions, and multi-scale dimensional diagnostics.",
  "type": "object",

  "properties": {
    "diagnostic": {
      "type": "object",
      "properties": {
        "name": { "type": "string" },
        "category": { "type": "string" },
        "version": { "type": "string" },
        "summary": { "type": "string" },
        "purpose": { "type": "string" }
      },
      "required": ["name", "category", "version", "summary", "purpose"]
    },

    "context": {
      "type": "object",
      "properties": {
        "dimensional_definition": { "type": "string" },
        "global_role": { "type": "string" }
      },
      "required": ["dimensional_definition", "global_role"]
    },

    "agents": {
      "type": "object",
      "properties": {
        "primary": {
          "type": "array",
          "items": { "type": "string" }
        },
        "secondary": {
          "type": "array",
          "items": { "type": "string" }
        }
      },
      "required": ["primary", "secondary"]
    },

    "fields": {
      "type": "object",
      "properties": {
        "dimensional_shift_field": {
          "type": "array",
          "items": { "type": "string" }
        },
        "regime_transition_field": {
          "type": "array",
          "items": { "type": "string" }
        },
        "coherence_break_field": {
          "type": "array",
          "items": { "type": "string" }
        },
        "cross_domain_dimensional_field": {
          "type": "array",
          "items": { "type": "string" }
        }
      },
      "required": [
        "dimensional_shift_field",
        "regime_transition_field",
        "coherence_break_field",
        "cross_domain_dimensional_field"
      ]
    },

    "metrics": {
      "type": "object",
      "additionalProperties": { "type": "string" }
    },

    "scales": {
      "type": "object",
      "properties": {
        "micro": { "type": "string" },
        "meso": { "type": "string" },
        "macro": { "type": "string" },
        "mega": { "type": "string" }
      },
      "required": ["micro", "meso", "macro", "mega"]
    },

    "failure_modes": {
      "type": "object",
      "additionalProperties": { "type": "string" }
    },

    "cascades": {
      "type": "object",
      "additionalProperties": {
        "type": "array",
        "items": { "type": "string" }
      }
    },

    "diagnostics": {
      "type": "object",
      "properties": {
        "maps": {
          "type": "array",
          "items": { "type": "string" }
        }
      },
      "required": ["maps"]
    },

    "summary": {
      "type": "object",
      "properties": {
        "core_capabilities": {
          "type": "array",
          "items": { "type": "string" }
        },
        "role": { "type": "string" }
      },
      "required": ["core_capabilities", "role"]
    }
  },

  "required": [
    "diagnostic",
    "context",
    "agents",
    "fields",
    "metrics",
    "scales",
    "failure_modes",
    "cascades",
    "diagnostics",
    "summary"
  ]
}
