{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Resonance Envelope Schema (Atmosphere Module)",
  "description": "Schema for validating resonance_envelope.json — harmonic boundaries, coherence shells, wave alignment limits, teleconnection resonance envelopes.",
  "type": "object",

  "properties": {
    "envelope": {
      "type": "object",
      "properties": {
        "name": { "type": "string" },
        "category": { "type": "string" },
        "version": { "type": "string" },
        "summary": { "type": "string" },
        "purpose": { "type": "string" }
      },
      "required": ["name", "category", "version", "summary", "purpose"]
    },

    "components": {
      "type": "object",
      "properties": {
        "boundary_layer": { "type": "string" },
        "stability_shell": { "type": "string" },
        "resonance_potential": { "type": "string" }
      },
      "required": [
        "boundary_layer",
        "stability_shell",
        "resonance_potential"
      ]
    },

    "sources": {
      "type": "object",
      "properties": {
        "composition": { "type": "string" },
        "forcing": { "type": "string" },
        "dynamics": { "type": "string" },
        "thermodynamics": { "type": "string" },
        "hydrospheric_coupling": { "type": "string" },
        "regime_transitions": { "type": "string" },
        "resonance_coherence": { "type": "string" }
      },
      "required": [
        "composition",
        "forcing",
        "dynamics",
        "thermodynamics",
        "hydrospheric_coupling",
        "regime_transitions",
        "resonance_coherence"
      ]
    },

    "signatures": {
      "type": "array",
      "items": { "type": "string" }
    },

    "agents": {
      "type": "object",
      "properties": {
        "primary": {
          "type": "array",
          "items": { "type": "string" }
        },
        "secondary": {
          "type": "array",
          "items": { "type": "string" }
        }
      },
      "required": ["primary", "secondary"]
    },

    "fields": {
      "type": "object",
      "properties": {
        "harmonic_boundary_field": {
          "type": "array",
          "items": { "type": "string" }
        },
        "wave_alignment_field": {
          "type": "array",
          "items": { "type": "string" }
        },
        "teleconnection_envelope_field": {
          "type": "array",
          "items": { "type": "string" }
        },
        "coherence_shell_field": {
          "type": "array",
          "items": { "type": "string" }
        }
      },
      "required": [
        "harmonic_boundary_field",
        "wave_alignment_field",
        "teleconnection_envelope_field",
        "coherence_shell_field"
      ]
    },

    "scales": {
      "type": "object",
      "properties": {
        "micro": { "type": "string" },
        "meso": { "type": "string" },
        "macro": { "type": "string" },
        "mega": { "type": "string" }
      },
      "required": ["micro", "meso", "macro", "mega"]
    },

    "cascades": {
      "type": "object",
      "additionalProperties": {
        "type": "array",
        "items": { "type": "string" }
      }
    },

    "overlays": {
      "type": "array",
      "items": { "type": "string" }
    },

    "summary": {
      "type": "object",
      "properties": {
        "core_capabilities": {
          "type": "array",
          "items": { "type": "string" }
        },
        "role": { "type": "string" }
      },
      "required": ["core_capabilities", "role"]
    }
  },

  "required": [
    "envelope",
    "components",
    "sources",
    "signatures",
    "agents",
    "fields",
    "scales",
    "cascades",
    "overlays",
    "summary"
  ]
}
