{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Continuity Diagnostic Schema (Atmosphere Module)",
  "description": "Schema for validating continuity_diagnostic.json — temporal coherence, regime memory, oscillation periodicity, drift accumulation, and teleconnection continuity diagnostics.",
  "type": "object",

  "properties": {
    "diagnostic": {
      "type": "object",
      "properties": {
        "name": { "type": "string" },
        "category": { "type": "string" },
        "version": { "type": "string" },
        "summary": { "type": "string" },
        "purpose": { "type": "string" }
      },
      "required": ["name", "category", "version", "summary", "purpose"]
    },

    "context": {
      "type": "object",
      "properties": {
        "continuity_definition": { "type": "string" },
        "global_role": { "type": "string" }
      },
      "required": ["continuity_definition", "global_role"]
    },

    "agents": {
      "type": "object",
      "properties": {
        "primary": {
          "type": "array",
          "items": { "type": "string" }
        },
        "secondary": {
          "type": "array",
          "items": { "type": "string" }
        }
      },
      "required": ["primary", "secondary"]
    },

    "fields": {
      "type": "object",
      "properties": {
        "temporal_coherence_field": {
          "type": "array",
          "items": { "type": "string" }
        },
        "regime_memory_field": {
          "type": "array",
          "items": { "type": "string" }
        },
        "oscillation_periodicity_field": {
          "type": "array",
          "items": { "type": "string" }
        },
        "drift_accumulation_field": {
          "type": "array",
          "items": { "type": "string" }
        },
        "teleconnection_continuity_field": {
          "type": "array",
          "items": { "type": "string" }
        }
      },
      "required": [
        "temporal_coherence_field",
        "regime_memory_field",
        "oscillation_periodicity_field",
        "drift_accumulation_field",
        "teleconnection_continuity_field"
      ]
    },

    "metrics": {
      "type": "object",
      "additionalProperties": { "type": "string" }
    },

    "scales": {
      "type": "object",
      "properties": {
        "micro": { "type": "string" },
        "meso": { "type": "string" },
        "macro": { "type": "string" },
        "mega": { "type": "string" }
      },
      "required": ["micro", "meso", "macro", "mega"]
    },

    "failure_modes": {
      "type": "object",
      "additionalProperties": { "type": "string" }
    },

    "cascades": {
      "type": "object",
      "additionalProperties": {
        "type": "array",
        "items": { "type": "string" }
      }
    },

    "diagnostics": {
      "type": "object",
      "properties": {
        "outputs": {
          "type": "array",
          "items": { "type": "string" }
        }
      },
      "required": ["outputs"]
    },

    "summary": {
      "type": "object",
      "properties": {
        "core_capabilities": {
          "type": "array",
          "items": { "type": "string" }
        },
        "role": { "type": "string" }
      },
      "required": ["core_capabilities", "role"]
    }
  },

  "required": [
    "diagnostic",
    "context",
    "agents",
    "fields",
    "metrics",
    "scales",
    "failure_modes",
    "cascades",
    "diagnostics",
    "summary"
  ]
}
