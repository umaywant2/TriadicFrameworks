{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Dimensional Trace Schema (Atmosphere Module)",
  "description": "Schema for validating dimensional_trace.json — dimensional events, regime shifts, structural transitions, coherence breaks, and multi-scale dimensional sequences.",
  "type": "object",

  "properties": {
    "trace": {
      "type": "object",
      "properties": {
        "name": { "type": "string" },
        "category": { "type": "string" },
        "version": { "type": "string" },
        "summary": { "type": "string" },
        "purpose": { "type": "string" }
      },
      "required": ["name", "category", "version", "summary", "purpose"]
    },

    "events": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "id": { "type": "string" },
          "description": { "type": "string" },
          "sequence": {
            "type": "array",
            "items": { "type": "string" }
          }
        },
        "required": ["id", "description", "sequence"]
      }
    },

    "signatures": {
      "type": "array",
      "items": { "type": "string" }
    },

    "agents": {
      "type": "object",
      "properties": {
        "primary": {
          "type": "array",
          "items": { "type": "string" }
        },
        "secondary": {
          "type": "array",
          "items": { "type": "string" }
        }
      },
      "required": ["primary", "secondary"]
    },

    "scales": {
      "type": "object",
      "properties": {
        "micro": { "type": "string" },
        "meso": { "type": "string" },
        "macro": { "type": "string" },
        "mega": { "type": "string" }
      },
      "required": ["micro", "meso", "macro", "mega"]
    },

    "fields": {
      "type": "object",
      "properties": {
        "dimensional_shift_field": {
          "type": "array",
          "items": { "type": "string" }
        },
        "regime_transition_field": {
          "type": "array",
          "items": { "type": "string" }
        },
        "coherence_break_field": {
          "type": "array",
          "items": { "type": "string" }
        },
        "cross_domain_dimensional_field": {
          "type": "array",
          "items": { "type": "string" }
        }
      },
      "required": [
        "dimensional_shift_field",
        "regime_transition_field",
        "coherence_break_field",
        "cross_domain_dimensional_field"
      ]
    },

    "cascades": {
      "type": "object",
      "additionalProperties": {
        "type": "array",
        "items": { "type": "string" }
      }
    },

    "summary": {
      "type": "object",
      "properties": {
        "core_capabilities": {
          "type": "array",
          "items": { "type": "string" }
        },
        "role": { "type": "string" }
      },
      "required": ["core_capabilities", "role"]
    }
  },

  "required": [
    "trace",
    "events",
    "signatures",
    "agents",
    "scales",
    "fields",
    "cascades",
    "summary"
  ]
}
