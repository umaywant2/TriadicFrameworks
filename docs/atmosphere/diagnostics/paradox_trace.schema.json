{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Paradox Trace Schema (Atmosphere Module)",
  "description": "Schema for validating paradox_trace.json — boundary conflict events, mixed-regime coexistence, shear tension, thermal instability, and transition-ready structures.",
  "type": "object",

  "properties": {
    "trace": {
      "type": "object",
      "properties": {
        "name": { "type": "string" },
        "category": { "type": "string" },
        "version": { "type": "string" },
        "summary": { "type": "string" },
        "purpose": { "type": "string" }
      },
      "required": ["name", "category", "version", "summary", "purpose"]
    },

    "events": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "id": { "type": "string" },
          "description": { "type": "string" },
          "sequence": {
            "type": "array",
            "items": { "type": "string" }
          }
        },
        "required": ["id", "description", "sequence"]
      }
    },

    "signatures": {
      "type": "array",
      "items": { "type": "string" }
    },

    "agents": {
      "type": "object",
      "properties": {
        "primary": {
          "type": "array",
          "items": { "type": "string" }
        },
        "secondary": {
          "type": "array",
          "items": { "type": "string" }
        }
      },
      "required": ["primary", "secondary"]
    },

    "scales": {
      "type": "object",
      "properties": {
        "micro": { "type": "string" },
        "meso": { "type": "string" },
        "macro": { "type": "string" },
        "mega": { "type": "string" }
      },
      "required": ["micro", "meso", "macro", "mega"]
    },

    "fields": {
      "type": "object",
      "properties": {
        "primary_paradox_field": {
          "type": "array",
          "items": { "type": "string" }
        },
        "frontal_paradox_field": {
          "type": "array",
          "items": { "type": "string" }
        },
        "cross_domain_paradox_field": {
          "type": "array",
          "items": { "type": "string" }
        },
        "oscillation_paradox_field": {
          "type": "array",
          "items": { "type": "string" }
        }
      },
      "required": [
        "primary_paradox_field",
        "frontal_paradox_field",
        "cross_domain_paradox_field",
        "oscillation_paradox_field"
      ]
    },

    "cascades": {
      "type": "object",
      "additionalProperties": {
        "type": "array",
        "items": { "type": "string" }
      }
    },

    "summary": {
      "type": "object",
      "properties": {
        "core_capabilities": {
          "type": "array",
          "items": { "type": "string" }
        },
        "role": { "type": "string" }
      },
      "required": ["core_capabilities", "role"]
    }
  },

  "required": [
    "trace",
    "events",
    "signatures",
    "agents",
    "scales",
    "fields",
    "cascades",
    "summary"
  ]
}
