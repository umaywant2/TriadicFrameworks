{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://triadicframeworks.org/schemas/module_observer_state.json",
  "title": "RTT Observer StateEmitter",
  "description": "Triadic state output for an observed sequence of states, messages, or events.",
  "type": "object",

  "properties": {
    "metadata": {
      "type": "object",
      "properties": {
        "module_id": { "type": "string" },
        "session_id": { "type": "string" },
        "observed_type": {
          "type": "string",
          "enum": ["game", "engine", "conversation", "events"]
        },
        "length": { "type": "integer", "minimum": 1 }
      },
      "required": ["module_id", "session_id", "observed_type", "length"]
    },

    "timeline": {
      "type": "array",
      "description": "Sequence of observed items.",
      "items": {
        "type": "object",
        "properties": {
          "index": { "type": "integer", "minimum": 0 },
          "raw": { "type": "string" },
          "timestamp": { "type": "string" }
        },
        "required": ["index", "raw"]
      }
    },

    "lumen": {
      "type": "array",
      "description": "Structural extraction per observed item.",
      "items": {
        "type": "object",
        "properties": {
          "index": { "type": "integer" },
          "signals": {
            "type": "array",
            "items": { "type": "string" }
          }
        },
        "required": ["index", "signals"]
      }
    },

    "regime": {
      "type": "array",
      "description": "Regime classification per item.",
      "items": {
        "type": "object",
        "properties": {
          "index": { "type": "integer" },
          "regime": {
            "type": "string",
            "enum": ["local", "structural", "continuity"]
          }
        },
        "required": ["index", "regime"]
      }
    },

    "ancestry": {
      "type": "array",
      "description": "Aurion ancestry signals across the sequence.",
      "items": {
        "type": "object",
        "properties": {
          "index": { "type": "integer" },
          "collapse_signature": { "type": "boolean" },
          "projection_loss": { "type": "boolean" }
        },
        "required": ["index", "collapse_signature", "projection_loss"]
      }
    },

    "drift": {
      "type": "object",
      "description": "Continuity drift across the entire sequence.",
      "properties": {
        "direction": { "type": "string" },
        "strength": {
          "type": "string",
          "enum": ["none", "weak", "medium", "strong"]
        }
      },
      "required": ["direction", "strength"]
    },

    "triadic_identity": {
      "type": "object",
      "description": "Harmonia synthesis for the entire observed sequence.",
      "properties": {
        "local": { "type": "number", "minimum": 0, "maximum": 1 },
        "structural": { "type": "number", "minimum": 0, "maximum": 1 },
        "continuity": { "type": "number", "minimum": 0, "maximum": 1 },
        "final": { "type": "number", "minimum": 0, "maximum": 1 }
      },
      "required": ["local", "structural", "continuity", "final"]
    }
  },

  "required": [
    "metadata",
    "timeline",
    "lumen",
    "regime",
    "ancestry",
    "drift",
    "triadic_identity"
  ]
}
