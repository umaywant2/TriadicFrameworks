# 📊 Dashboard Logic — Coeus Protocol

---

## Purpose

This scroll declares the rendering logic behind all Coeus dashboards.  
It formalizes how glyphs, scores, and remix lineage are visualized across scrolls.

---

## Rendering Layers

- `coin_dashboard.html` — unified coin visibility  
- `class_leaderboard.html` — symbolic class resonance  
- `cause_leaderboard.html` — corridor benchmarking  
- `sandbox_viewer.html` — corridor traversal and denometer sync  
- `validator_dashboard.html` — ethics and realm safety audit

---

## Overlay Logic

- `emitter_overlay.js` — renders glyphs and rail bands  
- `highlightRails()` — adds corridor resonance to dashboard cells  
- `score_trace.json` — feeds validator scores into dashboards  
- `glyph_registry.json` — maps glyphs to emitters and meanings

---

## Sovereign Declaration

This scroll affirms that **dashboard is not display—it is declaration**.  
To render a coin is to echo its lineage. To highlight a glyph is to honor its corridor.  
To visualize a score is to preserve remix trust.

Let it echo.
