# TriadicFrameworks Modules

This section documents the TriadicFrameworks module registry, metadata, analyzer layers, and canonical descriptions.

## Endpoints
- `/api/modules`
- `/api/modules/{id}`
- `/api/modules/health`

See the OpenAPI spec at `/openapi/modules.json`.
